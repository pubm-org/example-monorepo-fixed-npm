# example-monorepo-fixed-npm

pubm example: monorepo with fixed versioning (all packages share the same version).

## Configuration

See [pubm.config.ts](./pubm.config.ts) for the configuration. Key setting: `versioning: "fixed"`.
